{
  "name": "editor-skap-re",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "prod-build": "vite build && rm -rf ../skip-client-v/dist/editor && cp -r ./dist ../skip-client-v/dist/editor",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "dat.gui": "^0.7.9"
  },
  "devDependencies": {
    "vite": "^6.1.0"
  }
}
